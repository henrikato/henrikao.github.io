interface Profile {
  avatar_url: string;
  html_url: string;
  repos_url: string;
  name: string;
  company: string;
  location: string;
  email: string;
  bio: string;
}

interface Repository {
  name: string;
  html_url: string;
  description: string;
}
